// Program sums two numbers

#include <stdio.h>

int main() {
    int num1, num2, sum;

    printf("Enter the first num: ");
    scanf("%d", &num1);

    printf("Enter the first num: ");
    scanf("%d", &num2);

    sum = num1 + num2;
    printf("the sum is %d\n", sum);

    return 0;
}