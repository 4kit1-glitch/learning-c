//creating variables in c

#include <stdio.h>

int main() {

    // these are interger type varaibles 
    int number;
    int age;
    int phone_number;

    // there are other types which include float , double and char
    // float is used for decimal numbers
    // double is a very large float 
    // char are meant for character types

    /* char type
    the char type is a very important type as it helps us store characters
    it only enables the storage of a single letter but its not a limitation to the type
    it goes along with the getchar() and putchar() function which does what their names inscribes
    */
   char letter;

   /*
   float and double type are used to store  floating point numbers
   the float is not as large as the double
   the double type enables the use of scientific points that is the exponent and others
   but req a better way to access them
   */
   float average;
   double sqrt;

   /*other types in c
   other types in c includes the long, long double, long long, short
   which are used in large programs when controlling the size of input or operationsize
   */

   /*stack overflow and underflow
   stack overflow occurs when your trying to store a value too large in a varaible 
   stack underflow occurs if your trying to access an empty variable
   */


    return 0;
}